services:
  ui:
    build:
      context: ./ui/test
      dockerfile: Dockerfile
    ports:
      - 5173:5173

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
      - 3001:3001
